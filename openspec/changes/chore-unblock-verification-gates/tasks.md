# Tasks: chore — unblock verification gates

> **Status**: tasks
> **Project key**: `b_TTzk1b8KwYH`
> **Change name**: `chore-unblock-verification-gates`
> **Date**: 2026-06-02
> **Depends on**: `openspec/changes/chore-unblock-verification-gates/proposal.md`

## Forecast recap

~16 líneas manuales + lockfile regenerado. Cero nuevos tests, cero cambios visuales, cero impacto de runtime. Single PR, banda `<50` (lejos del budget de 400). Branch: `chore/unblock-verification-gates`. No chained PR.

## Work-unit commit plan (per `work-unit-commits` skill)

1. `fix(services): add missing modality and icon to second service entry` — completa el objeto en `components/services.tsx:53`.
2. `chore(eslint): add eslint and eslint-config-next with flat config` — suma deps + crea `eslint.config.mjs` + lockfile.

Dos commits. Cada uno resuelve un gate independientemente. Si uno rompe, el otro se puede mergear de todas formas.

---

## Phase 1: Type fix

### Task 1.1 — Complete the second `ServiceDetail` entry in `components/services.tsx`

- **id**: 1.1
- **phase**: 1
- **title**: Type completeness fix in services.tsx
- **files**:
  - `components/services.tsx` (modify, +2 lines)
- **acceptance_criteria**:
  - El objeto en la línea 53 incluye `modality: "Sesión de Charla"` e `icon: Heart`.
  - `npx tsc --noEmit` no reporta `TS2739` en `components/services.tsx:53`.
  - El render del service en el JSX sigue funcionando (no se introduce un componente inválido en runtime).
  - Sin otros cambios en el archivo (diff mínimo).
- **estimated_lines**: +2
- **dependencies**: none
- **work_unit_commits**:
  - `fix(services): add missing modality and icon to second service entry`
- **risks**:
  - "Sesión de Charla" puede no ser la copy final → aceptable, es decisión de contenido. El type-fix funciona con cualquier string.
  - `Heart` puede no ser el ícono de diseño correcto → aceptable, fácil de cambiar después. El type-fix funciona con cualquier `LucideIcon`.

---

## Phase 2: ESLint setup

### Task 2.1 — Add `eslint` + `eslint-config-next` to devDependencies

- **id**: 2.1
- **phase**: 2
- **title**: Install eslint and eslint-config-next
- **files**:
  - `package.json` (modify, +2 lines in devDependencies)
  - `pnpm-lock.yaml` (regenerated by `pnpm install`)
- **acceptance_criteria**:
  - `devDependencies` incluye `eslint` y `eslint-config-next`.
  - Versiones compatibles con `next@16.2.6` (preferentemente 16.x; fallback documentado a 15.x si 16.x no existe).
  - `pnpm install` completa sin errores.
  - `node_modules/.bin/eslint` existe después de instalar.
- **estimated_lines**: +2 in package.json; +N in pnpm-lock.yaml (no contado)
- **dependencies**: none (puede correr en paralelo con 1.1)
- **work_unit_commits**:
  - parte del commit `chore(eslint): add eslint and eslint-config-next with flat config`
- **risks**:
  - Versión de `eslint-config-next` no alineada con Next 16 → fallback documentado.
  - Conflicto de peer deps → `pnpm install` debe reportarlo; en ese caso documentar y resolver con `--legacy-peer-deps` si es necesario (NO ideal, pero aceptable como último recurso).

### Task 2.2 — Create `eslint.config.mjs` (flat config for Next 16)

- **id**: 2.2
- **phase**: 2
- **title**: Add ESLint flat config
- **files**:
  - `eslint.config.mjs` (new, ~12 lines)
- **acceptance_criteria**:
  - El archivo existe en la raíz del repo.
  - Usa `FlatCompat` de `@eslint/eslintrc` para reusar `eslint-config-next`.
  - Extiende `next/core-web-vitals` y `next/typescript`.
  - `pnpm lint` corre sin `sh: eslint: command not found`.
- **estimated_lines**: +12
- **dependencies**: 2.1 (necesita `eslint` instalado para que `pnpm lint` corra; el archivo puede crearse en cualquier orden)
- **work_unit_commits**:
  - parte del commit `chore(eslint): add eslint and eslint-config-next with flat config`
- **risks**:
  - Si `@eslint/eslintrc` no está disponible implícitamente vía `eslint-config-next`, hay que agregarlo a devDeps. Documentar si pasa.
  - Si `next/typescript` extend no existe en la versión instalada, fallback a sólo `next/core-web-vitals`.

### Task 2.3 — Verify the new gates pass

- **id**: 2.3
- **phase**: 2
- **title**: Run quality gates
- **files**: (none — verification only)
- **acceptance_criteria**:
  - `npx tsc --noEmit` pasa.
  - `pnpm lint` corre (puede reportar warnings; documentar).
  - `pnpm build` sigue pasando.
- **estimated_lines**: 0
- **dependencies**: 1.1, 2.1, 2.2
- **work_unit_commits**:
  - (no commit — la verificación se reporta en el return envelope del apply)
- **risks**:
  - `pnpm lint` puede reportar errores preexistentes en otros archivos → documentar; NO expandir scope.

---

## Risks (consolidated)

- **LOW**: Copy "Sesión de Charla" puede no ser la final. Aceptable, decisión de contenido.
- **LOW**: `Heart` puede no ser el ícono de diseño correcto. Aceptable.
- **MEDIUM**: Versión de `eslint-config-next` puede no estar alineada con Next 16. Mitigación: fallback documentado.
- **MEDIUM**: `pnpm lint` puede exponer errores preexistentes en otros archivos. Mitigación: documentar; NO arreglar en este cambio.
- **LOW**: `@eslint/eslintrc` puede no estar disponible implícitamente. Mitigación: agregar a devDeps si hace falta.

## Out-of-scope

- Cambios de reglas de lint más allá del mínimo.
- Migración a Prettier.
- Tests (no hay test runner).
- Refactor de `services.tsx` más allá del fix.
- Cambios al type `ServiceDetail`.
- Cualquier otro ajuste en `package.json`.
- Tocar `feat/dark-mode` (eso se rebasea después en una fase aparte).

## Definition of done

- [x] Task 1.1 committeado (`fix(services): add missing modality and icon to second service entry`)
- [x] Task 2.1 + 2.2 committeados en un solo commit (`chore(eslint): add eslint and eslint-config-next with flat config`)
- [x] `npx tsc --noEmit` pasa
- [x] `pnpm lint` corre (warnings documentados si los hay)
- [x] `pnpm build` sigue pasando
- [x] `git diff main..chore/unblock-verification-gates` muestra sólo los archivos listados
- [x] No `package.json` modifications fuera de las 2 líneas en devDeps
- [x] No commits con AI co-author
- [x] Conventional Commits respetados

## Next phase

`sdd-apply` para implementar y commitear. Después: rebase `feat/dark-mode` sobre esta rama, y `sdd-verify` sobre dark-mode con gates limpios.
